<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:background="#E1F0DA"
    tools:context=".Security.QuenMatKhau">

    <!-- Logo -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:src="@drawable/daisy"
        android:contentDescription="@null"
        android:scaleType="centerInside"
        android:layout_marginBottom="20dp" />

    <!-- Tiêu đề -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="QUÊN MẬT KHẨU"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#99BC85"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <!-- Mô tả -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nhập email của bạn để đặt lại mật khẩu"
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <!-- Ô nhập Email -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        app:boxBackgroundMode="outline"
        app:boxStrokeColor="#99BC85"
        app:hintTextColor="#99BC85">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/email_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Email"
            android:inputType="textEmailAddress"
            android:padding="10dp"
            tools:ignore="TouchTargetSizeCheck" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Nút Reset Password -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/reset_password_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Gửi Email Đặt Lại"
        android:textColor="#FFFFFF"
        app:backgroundTint="#99BC85"
        android:layout_marginBottom="16dp" />

    <!-- Thông báo -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Kiểm tra hộp thư để nhận hướng dẫn đặt lại."
        android:textSize="14sp"
        android:textColor="#99BC85"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <!-- Nút Back to Login -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/back_to_login_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Quay Lại Đăng Nhập"
        android:textColor="#99BC85"
        app:backgroundTint="@android:color/transparent"
        app:strokeColor="#99BC85"
        app:strokeWidth="1dp" />

</LinearLayout>